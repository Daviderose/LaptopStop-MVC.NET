<h1>@ViewBag.title</h1>

<form method="post">

    @foreach (var laptop in ViewBag.laptops)
    { 
       <input type="checkbox" name="laptopIds" id="@laptop.ID" value="@laptop.ID" />
       <label for="@laptop.ID">@laptop.Name</label>
       <br />
    }

    <input type="submit" value="Remove Selected Laptop(s)" />
</form>
